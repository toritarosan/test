# 会議録アーカイブ — 今後の構想メモ（ロードマップ）

このファイルは、将来やりたい方向を忘れないための覚書。環境は都度リセットされるため、
口頭ではなくここに記録しておく。実装はまだ着手していない（＝アイデア段階）。

## 現状（2026-07時点）
- 静的サイト（GitHub Pages、`wiki/` 配下、`.nojekyll` で素のHTML配信）。
- 原典「小金井市議会 非公式会議録」（Gijiroku1）を再編集した**読み取り専用アーカイブ**。
- サイト表示名は **「小金井市 議案・審査案件マッシュアップ」（非公式）**（旧称「会議録アーカイブ」から2026-07-04変更・本番反映済み）。
- 議題まとめ 28本（うち6月分は多エージェント＋`verify.py`検証パイプラインで生成）。
- デザインは **タイル案2（密敷きタイル＋明るいヘッダー・アンバーの「会」マーク）を全ページ本番適用済み**。
- **連想ナビ「議論の海サーフ」Phase 1 を本番公開済み**（`wiki/surf/`・ホームのクイックリンクから。詳細は下記「実装記録」）。
- 正確性の核：`POLICY.md`（編集規程）＋ `verify.py`（原典突合の機械検証）＋ 人手承認。

## デザイン検討メモ：トップのタイル置き換え「メモ用紙案（note-1）」
- **状態**：モックアップのみ作成。本番未適用（＝アイデア段階／ユーザー確認待ち）。
- **きっかけ**：ユーザーが「画鋲で留めたクリーム色のメモ用紙（罫線入り）」の画像を提示し、
  トップの議題まとめタイルをそれ風に置き換えられるか、と依頼。
- **モックの場所**：`wiki/mockups/note-1.html`（実データ28本から生成・自己完結。CSSはインラインで内包）。
  比較用に現行の `wiki/mockups/tile-2.html` と並べ、上部の比較バーで行き来できる。
- **見た目**：クリーム紙＋淡い水色の罫線（`repeating-linear-gradient` で26px間隔）＋
  上端中央に**カテゴリ色の画鋲**（`.npin`、`radial-gradient` の球）。紙を `transform:rotate` で
  少し傾け、hoverで真っ直ぐ起き上がって浮く。
- **2バリアント**：
  1. **主案＝クリーム紙＋色画鋲**（画像に忠実。色分けは画鋲が担う）← 推奨。
  2. 色つき用紙版（用紙自体をカテゴリ淡色 `--t` に）＝**淡すぎてクリーム紙と見分けにくく不採用寄り**。
- **本番反映のやり方（GOが出たら）**：`wiki/style.css` の `.tile` 一式（約40行）を note 用スタイルに
  差し替えるだけ。**`generate.py`・各HTMLは変更不要**（各タイルに `--c`（カテゴリ色）／`--t`（淡色）が
  既に埋まっているため、CSSだけで置換できる）。
- **未決事項／懸念（確信度：中）**：
  - 罫線と文字行は「雰囲気として」揃えている（完全一致ではない）。厳密に揃えると密度が落ちる。
  - 現行tile-2の「フラット・密敷き」から、掲示板的な柔らかい印象へテイストが変わる点の是非。
  - 調整余地：傾きの強さ／画鋲の大きさ・色/罫線の濃さ／紙の色。
- **再生成メモ**：モックは throwaway 生成スクリプト（scratchpad の `build_mock.py`、環境リセットで消える）で
  出力したが、**デザインの正本は committed の `note-1.html`（CSSインライン内包）**。作り直す場合はそれを参照。

## 検討：議論の海を「サーフィンする」連想ナビ（2026-07-04検討・未実装）
階層で情報を整理して見せるのではなく、会議・議員・議案番号・会期をリンクで横断して漂う
（昔のワードクラウド・リンク集のように）ナビへの転換案。25エージェント（リサーチ4→コンセプト5→
採点15票→統合1）で実リポジトリと POLICY §9 に即して実現可能性を検討した結果を記録。**未実装（GO待ち）。**

### 結論
- **できる。しかも派手なグラフ描画を作らなくても、純HTMLの相互リンク網だけで「サーフィン」は成立する**（最重要の発見）。
- 採点1位 **案2「全エンティティWiki化」（22.7/25）を土台**に、案3ワードクラウド（入口）と案1/案4の
  `graph.json`（監査用の共起グラフ実体）を接木。星座グラフ・サーフモードは**後乗せの装飾**に格下げ。
- 5案の採点：案2 Wikiバックリンク=22.7／案4 セレンディピティ・サーフ=21.7／案3 ワードクラウド=21.3／
  案1 星座グラフ=20.7／案5 テーマ地図=19.7（満点25）。全案とも静的サイト適合は満点。

### この企画の心臓部＝「根拠あるエッジだけ張る」（POLICY §9の生命線）
リンク＝連想を張る以上、それが「AIの意味的推論」や「編集者の主観」だと捏造・誘導になる。
**張れるのは原典に根拠のある機械的共起のみ**。チップの大小も機械カウントのみ。全ハブに
「連想＝関係の証明ではない」旨と原典リンクを常設する。

| エッジ | 根拠（generate.py上の出所） | 検証 | 表示ラベル |
|---|---|---|---|
| 会議⇄議員 | `members`（本文 h3「○○議員」抽出） | V3相当 | 「この会議に登壇」 |
| 議員⇄議員 | 同一会議への共登壇（`members`共起） | V3相当 | 「同じ会議に登壇」 |
| 会議⇄会議 | 同一議案/陳情番号（`bills`→V3と同一正規表現） | V3同一トークン | 「同じ議案第N号を扱う」 |
| 会議⇄会議 | 同一会期（`sessions`） | 原典indexメタ | 「同じ会期」 |
| 会議⇄カテゴリ | `cat`/`catmap` | 原典indexメタ | 「同じ会議種別」 |
| 記事⇄議員【最硬】 | `article["people"]` | **V3が原典本文へ逐語照合** | 「この記事に登場」 |
| 記事→会議【最硬】 | `article["src"]` | **verifyの照合先そのもの** | 「この記事の出典会議」 |
| ~~記事⇄記事~~ | ~~`others[:3]` 恣意スライス~~ | **なし＝撤去** | 共起件数順に置換 |
| ~~tags由来~~ | 編集ラベル・verify非検証 | **なし＝端点化禁止** | 無リンク＋「未検証」開示 |

### Phase 1：JSゼロで“サーフィン”を成立させる背骨
- 新規ハブ：`out/b/<slug>.html`（議案・陳情番号）／`out/s/<slug>.html`（会期）／`out/c/<slug>.html`（会議種別）
  ＋監査用 `out/graph.json`（nodes/edges・tags除外・概30–60KB）。
- generate.py 追加点：`makedirs` に `b/ s/ c/`。`people{}` と同型の逆引き辞書（`bills_index`/`session_index`/
  `cat_index`）を1ループ構築。番号 slug ヘルパー（V3と同一正規表現・**截断前**の `bills` を入力・番号部のみ
  `gian-N`/`chinjo-N` 等に slug 化）。`bill_page()`/`session_page()`/`category_page()` と共通の
  `backlinks_block()`「ここにリンクしている項目」。議員⇄議員チップ・会議→各ハブチップを追加（全チップにラベル文言）。
  記事の `others[:3]` を撤去し共起件数順ランキングに置換（共起ゼロ時は「同一カテゴリ最新N件」を公開閾値として明示）。
- 完了の目安：`python3 generate.py out` が exit 0（verify合格維持）／`c/` が5ファイル・`b/ s/` 非空／
  `grep others\[:3\] generate.py` が0件／graph.json 全エッジ `rel!='tag'`。

### 実装記録：Phase 1 実装済み（2026-07-04）
- **状態**：**実装・検証・公開済み**（ライブ層）。generate.py 統合はコード実装済み・ロジック検証済み（本番実行は原典 src/ のある環境で）。
- **配置の変更（ROADMAP から意図的に逸脱）**：ハブを `out/b/ out/s/ out/c/` の**トップレベルではなく `surf/` サブディレクトリ**に集約（`wiki/surf/{index.html, m/, p/, b/, c/, s/, a/, graph.json, surf.css}`）。
  理由：この環境に generate.py の入力（原典 src/62本＋index2本）が無く generate.py を実行/検証できないため、
  コミット済み検証データ（search.json/list.html/articles.py）から `build_surf.py` で**今すぐ生成・検証・ライブ反映**できる形にした。
  両経路が同じ場所 `surf/` を出力するので、後日 src/ のある環境で generate.py を回すと截断前 bills 版で自動上書きされ、乖離しない。
- **単一ロジック源**：`wiki/surf_core.py`（エッジ導出＝機械的共起のみ＋ページ描画＋graph.json）。
  `build_surf.py`（ライブ）と `generate.py`（本番）が**同一の surf_core を使用**＝「どのエッジを張るか」は必ず一致（§9の生命線を一箇所に集約）。
- **エッジ実装**：会議⇄議員(member)／議員⇄議員(co-appear・重み=共登壇会議数)／会議⇄議案番号(bill・V3同一正規表現)／
  会議⇄会期(session)／会議⇄カテゴリ(category)／記事⇄議員(article-person・V3)／記事→会議(article-src)／記事⇄議案(article-bill)。
  **tags は端点にしない**（無リンク＋「編集ラベル・未検証」開示）。graph.json は `rel!='tag'` を assert で保証。
- **generate.py 側の変更**：surf_core import ／ サーフ層 OUT/surf/ 書き出し ／ 会議・議員・記事ページに「🌊 議論の海をたどる」導線 ／
  記事の `others[:3]` 恣意スライスを撤去し**共起（登場議員・出典会議の共有数）順**に置換（共起ゼロ時のみ同一カテゴリを明示フォールバック）。
- **ホーム導線**：`index.html` クイックリンク先頭に「🌊 議論の海サーフ」。generate.py の index_body にも**同一スニペット**を追加（パイプライン再現性）。
- **検証済み事実**：`build_surf.py` exit 0（会議62・議員24・議案60種・会期3・記事28／182ノード・544エッジ）／
  surf/ 内リンク1810件**切れ0**（本サイトへの `../../` 外部リンク含む）／graph.json tagエッジ0・44.7KB／
  `python3 -m py_compile generate.py` 成功／`grep others\[:3\] generate.py` 0件／`related_articles()` を全28記事でスタンドアロン検証（自己参照なし・27件共起/1件フォールバック）。
- **未反映（既知・要 src/）**：本体 m/p/a ページの導線リンクと記事の関連順の変更は generate.py 実行時に反映＝**現状のライブの m/p/a ページには未反映**。
  ライブに出ているのは `surf/` 一式・ホーム導線・style.css のみ。
- **既知の制限**：ライブ層の議案接続は search.json `bills[:8]` 由来で9会議分を一部取りこぼす（index に開示済み）。generate.py 経路は截断前 `bills[:30]` で解消。
- **旧サンプル**：`wiki/mockups/surf/`（検討時のプロト）は本番 `wiki/surf/` に発展・置換。mockup は履歴として残置。

### Phase 2：上乗せの可視化（Progressive Enhancement・JSは装飾のみ）
- 入口＝トップのワードクラウド（案3）：議員・議案番号・会議種別を機械カウントでサイズ付けした純HTML+CSS。
  font-size 写像は**単一の決定的な式**を定数化し POLICY に明記（監査可能化）。tags は雲に混ぜず別枠で「未検証」開示。
- 任意＝星座SVG（案1、座標はビルド時に固定シードで確定し graph.json に焼込・実行時再計算なし）／
  サーフ2ボタン（案4、`surf.js` は graph.json を読むだけ・実行時に新エッジを生成しない）。
- no-JS：背骨（Phase 1）が常に DOM 内に存在し、Tab到達・クローラ巡回・SR読み上げが全ノードで成立。

### 未解決リスク・要確認
1. tags 無リンクは「押せない不便」に映る恐れ（確信度：高）。「編集ラベル・未検証」開示文の設計が要る。
2. POLICY §9 本文に「エッジ＝機械的共起のみ・閾値公開・再生成可能」条文を追記＝**人間の最終承認事項**（文案は用意可、確信度：高）。
3. `bills` の截断（`[:30]`/search `[:8]`）と番号 slug の全角半角ゆれ→截断前から別集計＋`normalize()`相当で正規化（確信度：中）。
4. graph.json 再生成→CI差分照合は未実装（§9-2 の監査担保として別途必要、確信度：中）。
- 壊れうる隣接機能：`list.html` の絞り込みJSは `tr[data-cat]` のDOM構造に依存。Phase 1 は list.html を触らず分離する。

### 最初の一歩（依存の無い最小・可逆な着手）
番号インデックスが実データで作れるかだけを確認（新ファイル・HTML書き出しなし）：
```bash
cd wiki && python3 -c "
import re
from generate import docs   # 既存 extract() 結果を再利用（import で全生成が走る点に注意→実装時は関数分離）
RE=re.compile(r'議案第\d+号|意見書案第\d+号|陳情第\d+号|請願第\d+号')
idx={}
for f,d in docs.items():
    for b in d['bills']:
        for g in RE.findall(b): idx.setdefault(g,set()).add(f)
print('numbers:',len(idx)); assert idx"
```
これが exit 0 で番号→会議数の辞書が非空なら、番号ハブが作れると実証。次は `bill_page()` 実装へ。

（詳細版はセッション内でアーティファクト化済み＝エッジ定義表・採点内訳・フェーズ図つき。
URLは次セッションで失効しうるので、本節の要約から再構成するか同じ検討プロセスを再実行する。）

## やりたい構想：参加型「リクエスト→検証済みカード」＋市民関心の可視化
サイト訪問者が「◯◯について知りたい」と投げると、しばらくして検証済みカードができる仕組み。
静的アーカイブを「生きた市民関心ボード」に進化させる。

### レイアウト案（左右分割）
- **左＝収録済み（検証済み・正式記録）**：現在の議題まとめ。AI生成＋`verify.py`＋人手承認を通った確定コンテンツ。
- **右＝リクエスト（生きた関心）**：訪問者が投げた話題。ここが市民の関心・トレンドの窓。
- **カードの“昇格”フロー**：リクエスト（右）→ 生成＋検証 → 人が承認 → 左に「収録」として移動。

### トレンド可視化（低リスクで“楽しい”部分）
- 生成物ではなく**リクエストそのものを集計**するのは原典照合の心配がなく安全。
  - 「今週よく聞かれた議題」ランキング／タグクラウド／「N人がこの話題を知りたがっています」等。
- 但し書きを明示：右側は「わざわざ聞いた訪問者」の関心であり、**市民全体の縮図ではない**。

## アーキテクチャ（静的サイトのままにする）
```
[① 依頼]  サイト or HF Space の入力欄「◯◯について作って」
   ↓（非同期・数分）
[② 生成＋検証]  コンピュート層（HF Spaces もしくは GitHub Actions）:
   1. 話題に関係する原典会議録を検索（RAG）
   2. POLICY.md に従いカードを下書き（LLM）
   3. verify.py で原典と機械照合  ← 必須ゲート。右側の生成物にも必ず適用
   4. 合格→採用／不合格→破棄 or 再試行／根拠なし→「該当なし・原典参照」
   ↓
[③ 反映]  合格カードを PR でリポジトリへ → 人が承認・マージ → Pages 再ビルドで公開
```
- **コンピュート層の候補**：
  - A. **HF Spaces**（ユーザーが挙げた案）：入力欄＋生成エンジン。秘密鍵・GitHubトークン保持可、PR作成も可。
  - B. **GitHub Actions のみ**：Issue/フォーム起点でCIが生成→PR。外部サービス不要・全部GitHub内で完結。
  - → **2026-07-03に決定を確定。下記「決定：採用アーキテクチャ」参照（HF Spacesは不採用、GitHub完結＋段階導入）。**

## 新規に必要な部品（今は無い）
1. **RAG検索**：話題→関連会議録の特定（62本をチャンク化しベクトル検索。FAISS/Chroma等）。← 最大の新規開発だが定番技術。
2. **生成モデル**：Claude API または HF のオープンモデル。
3. **書き込み権限**：Space→リポジトリへ push/PR するための GitHub トークン（シークレット格納）。
4. **モデレーション（最重要）**：公開リクエスト欄は荒らし・誹謗・誘導（特定議員を貶める生成 等）が来る。
   不適切語フィルタ＋**公開前の人手承認**必須。リクエスト文の表示にも承認が要る。
   ただし削除・却下は「荒らし・誹謗・違法・スパム」に限定し、**内容の立場を理由に消さない**（POLICY §9-2）。
   検証・モデレーションを通ったリクエストは恣意的に選別せず公開する（取捨選択バイアスの排除）。
5. **コスト制御**：1リクエスト＝LLM課金。レート制限＋類似リクエストのキャッシュ/集約。
6. **トレンド集計**：リクエストのログ集計・ランキング・タグ化。

## 正直な注意点（＝“非現実的か？”への答え）
- 技術的な難所は少ない（生成＋検証は既にある）。本当の勝負どころは **モデレーションとコスト管理**という運用面。
- 検証ゲートを右側にも必ず通すこと。飛ばすと今まで積み上げた正確性保証が崩れる。
- 初期はリクとが少なく右カラムが過疎になりがち → 運営側で種まき。

## おすすめの進め方（段階）
- **Phase 1（低リスク・すぐ楽しい）**：右＝リクエスト受付＋トレンド板（集計表示のみ）。生成はモデレーション待ち行列へ。
- **Phase 2**：承認された話題を verify.py で検証→カード化→人が承認→左に“昇格・収録”。
- 着手する場合の最初の一歩は、下記「決定：採用アーキテクチャ」内の該当項目を参照。

---

## 決定：採用アーキテクチャ（2026-07-03決定・未実装）

複数アーキテクチャ案（多エージェントでリサーチ4本→設計案3本→各案6基準×3票で独立採点→統合）を比較した結果、以下に決定。
**この決定は「アイデア」ではなく「採用方針」。実装自体はまだ着手していない。**

### 採点結果（6基準×3票平均、満点30点）
- 案C：段階最小案（Phase1はトレンド板のみ自動化・生成は完全手動・LLM鍵ゼロ）＝**24.0点（採用）**
- 案A：GitHub完結型（Issue駆動で生成まで自動化）＝21.3点
- 案B：HF Space＋GitHub PRハイブリッド＝20.7点（**不採用**：秘密鍵の二重管理と外部依存のコストが個人運用に見合わない）

### 結論
**案C（段階最小案）を土台に採用**し、Phase 2（検証→カード昇格の自動化）でのみ案Aの「却下理由コードの限定列挙＋承認と機械検証合格が揃えば例外なく機械的にPR化＋内容非依存の`budget.json`サーキットブレーカー」を接木する。HF Spacesは採用しない（GitHub完結）。

### Phase 1：受付＋トレンド板（LLM生成なし・秘密鍵不要）
- 新規ファイル：`.github/ISSUE_TEMPLATE/topic-request.yml`（話題投稿フォーム、却下基準を本文明記、`labels:["request"]`自動付与）／`.github/ISSUE_TEMPLATE/config.yml`／ラベル一式（`request` `approved` `rejected:spam` `rejected:abuse` `rejected:illegal` `rejected:personal-info` `rejected:duplicate` `published:<slug>`）／`.github/workflows/trend-build.yml`（cron、現状ゼロの`.github/workflows`への初追加）／`wiki/build_trend.py`／`wiki/trend.json`／`wiki/trend.js`
- データフロー：訪問者がIssue Forms投稿→collaboratorが人手でラベル付け（approved/rejected:<code>）→cronで`build_trend.py`がGitHub REST APIを読み取り集計→`trend.json`へ直接コミット→既存Pages配信が自動反映。生成は運用者が手元で`verify_one.py`→`integrate.py`→`generate.py`を実行し人力でPR。
- 最初の一歩：`.github/ISSUE_TEMPLATE/topic-request.yml`を追加するだけ（秘密鍵・既存コード変更不要）。完了の目安：Issuesの「New issue」でこのフォームが選択でき、投稿すると`request`ラベルが自動付与されること。

### Phase 2：検証→カード昇格（ここで初めてLLM APIキーを導入）
- 新規ファイル：`.github/workflows/draft-request.yml`（`ready-to-draft`ラベルがトリガー）／`wiki/rag_index.py`＋`wiki/bm25.py`（BM25索引、索引自体もJSONでコミットし監査対象にする）／`wiki/budget.json`（月次・日次上限のサーキットブレーカー）
- 既存の`verify_one.py`・`integrate.py`・`generate.py`のゲート構造は無改変で再利用。PRに含めるのはARTICLES_DRAFT（データ）のみでHTMLは含めない＝生成物の手編集禁止（POLICY §9-2）を維持。
- 検証が2回連続で不合格の場合は3回目を自動で試さず人手対応を要請して打ち切る（CLAUDE.mdの「同じ修正は2回まで」方針を踏襲）。

### モデレーション：却下理由コードは5種類に限定（これ以外を理由にできない）
`rejected:spam`（宣伝）／`rejected:abuse`（誹謗中傷）／`rejected:illegal`（違法・プライバシー侵害）／`rejected:personal-info`（個人情報）／`rejected:duplicate`（重複）。内容の立場・主張は却下理由にできないことをIssue Forms本文に明記する（POLICY §9-2）。承認ラベル＋verify合格が揃えば例外なく機械的にPR化し、どのリクエストをPR化するかに運用者の裁量を挟まない。

### コスト制御（Phase2のみ、仮値・要確認）
月間上限50件・日次上限10件（`wiki/budget.json`、`concurrency: group: budget-lock`で書き込みを直列化）／同一投稿者の同時未処理リクエスト上限3件／重複判定はGitHub純正reactionによる関心集約で代用（新規アルゴリズムは作らない）。

### 未解決のリスク（要検討・意図的に未対応）
1. **承認済みトピックの着手順序に恣意性が残る**：どの承認済み案件から手を付けるかは人手の裁量のまま。反論「それは§9-2が禁じる恣意性そのものでは」に対し、「未解決の残存リスクとして明記し、順序の強制機構化は今回のスコープ外」と回答済み。
2. reaction数ランキングはソックパペット（複数無料アカウントでの水増し）に弱い（対策未実装）。
3. cronの間隔とPagesビルド頻度のトレードオフは実測なし。
4. `budget.json`の同時書き込み排他制御は設計のみで未検証。
5. SLA・上限件数（48時間／50件／10件／3件）はすべて仮値、実運用前に要確認。

### 統合プラン全文
セッション内でアーティファクトとして生成済み（統合プランの完全版・スコア内訳・データフロー図つき）。次回セッションでURLが失われている場合は、本セクションの要約から再構成するか、同じ調査プロセス（リサーチ→設計案比較→採点→統合）を再実行する。

---
（Phase1・Phase2とも未実装。着手する場合はまず「Phase 1：最初の一歩」から。）
